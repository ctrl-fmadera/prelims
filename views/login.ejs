<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Preliminary Exam</title>
    <link rel="stylesheet" href="styles.css" />
</head>
<body>  
    <div class="container">
    <h1>Log In</h1>
    <% if (errorMessage) { %>
        <p><%= errorMessage %></p>
    <% } %>
    
    <form method="POST" action="/login">
        <label>Username: 
            <input name="username" required type="text" value="<%= username %>" />
        </label><br/>

        <label>Password: 
            <input name="password" required type="password" />
        </label><br/>

        <% if (authKeyRequired) { %>
            <label>Authentication Key:
                <input name="authKey" required type="text" />
            </label><br/>
        <% } %>

        <br /><a href="/login_success"><button type="submit"><%= authKeyRequired ? 'Submit Authentication Key' : 'Log In' %></button></a>
    </form>
    <div>
</body>
</html>