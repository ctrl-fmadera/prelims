<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Signup</title>
</head>
<body>
    <h1>Sign Up</h1>
    <% if (errorMessage) { %>
        <p style="color: red;"><%= errorMessage %></p>
    <% } %>

    <form method="POST" action="/signup">
        <label>Username: 
            <input name="username" required type="text" value="<%= username %>" />
        </label><br/>

        <label>Password: 
            <input name="password" required type="password" />
        </label><br/>

        <% if (ageRequired) { %>
            <label>Age:
                <input name="age" required type="number" min="1" />
            </label><br/>
        <% } %>

        <button type="submit"><%= ageRequired ? 'Submit Age' : 'Start Exam' %></button>
    </form>
</body>
</html>
