<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Preliminary Exam</title>
    <link rel="stylesheet" href="styles.css" />
</head>
<body>
    <div class="container">
    <h1>Sign Up</h1>

    <% if (errorMessage) { %>
        <p><%= errorMessage %></p>
    <% } %>

    <form method="POST" action="/signup">
        <label>Username: 
            <input name="username" required type="text" value="<%= username %>" />
        </label><br/>

        <label>Password: 
            <input name="password" required type="password" />
        </label><br/>

        <% if (ageRequired) { %>
            <label>Age:
                <input name="age" required type="number" min="1" />
            </label><br/>
        <% } %>

        <br /><button type="submit"><%= ageRequired ? 'Submit Age' : 'Start Exam' %></button>
    </form>
    </div>
</body>
</html>
